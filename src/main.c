#include <stdio.h> /* Temporary printf import */
#include "block.h"

int main(void) {
    /* For now, just a placeholder */
    Block test_block = {100, 200, BLOCK_SIZE};

    printf("Running main!\n");

    return 0;
}